## What & why

Describe the change and the reason for it.

## Checklist
- [ ] Tests pass (`cd engine && python -m pytest`)
- [ ] New fingerprints still pass `validate()` (if engine touched)
- [ ] Dashboard builds (`cd dashboard && npm run build`) (if UI touched)
- [ ] Docs updated if needed
